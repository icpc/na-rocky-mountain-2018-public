SET(maxn = 1000)
SET(maxk = 1000000000000000000)
INT(2, maxn, n) SPACE INT(1, maxk, k) NEWLINE
SET(fact = 1)
SET(good = 0)
SET(i = 1)
WHILE( i <= n && good == 0 )
  SET(fact = fact*i)
  IF( fact >= k ) 
    SET(good = 1)
  END
  SET(i = i+1)
END
EOF

