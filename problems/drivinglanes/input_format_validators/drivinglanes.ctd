SET(max = 1000000)
SET(maxn = 250)
SET(maxm = 250)
INT(1,maxn,n) SPACE INT(1,maxm,m) NEWLINE
INT(1,max,k) SPACE INT(1,max,r) NEWLINE
REP(n)
  INT(1,max) NEWLINE
END
REP(n-1)
  INT(1,max,l) SPACE INT(-max,max,c) NEWLINE
  ASSERT(l+m*c > 0)
END
EOF
